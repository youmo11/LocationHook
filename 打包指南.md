# 📦 LocationHook 打包指南

## ✅ 当前状态：源代码已完成

所有核心代码已准备就绪：
- ✅ LSPosed Hook核心 (LocationHookModule.java)
- ✅ 位置模拟引擎 (MockLocationProvider.java)
- ✅ 路线模拟器 (RouteSimulator.java)
- ✅ 高德地图集成 (AmapManager.java)
- ✅ 配置管理 (HookConfig.java)
- ✅ 工具类 (LocationConverter, XPreference, PermissionHelper)
- ✅ 主界面 (MainActivity.java + activity_main.xml)
- ✅ 权限清单 (AndroidManifest.xml)
- ✅ Xposed入口 (xposed_init)
- ✅ Gradle配置 (build.gradle, settings.gradle)

---

## 🚀 快速打包步骤

### 第一步：安装必要环境

#### 1. 安装JDK 17
```bash
# 下载地址：https://adoptium.net/
# 安装后验证：
java -version
```

#### 2. 安装Android Studio
```bash
# 下载地址：https://developer.android.com/studio
# 安装完成后打开，等待自动下载SDK
```

#### 3. 配置环境变量
```bash
# 在系统环境变量中添加：
JAVA_HOME=C:\Program Files\Eclipse Adoptium\jdk-17.0.8.101-hotspot
ANDROID_HOME=C:\Users\你的用户名\AppData\Local\Android\Sdk

# 在Path中添加：
%JAVA_HOME%\bin
%ANDROID_HOME%\platform-tools
```

---

### 第二步：配置高德API Key

1. 访问 [高德开放平台](https://lbs.amap.com/) 注册账号
2. 创建新应用
3. 添加Key：
   - 服务平台：Android平台
   - 应用名称：LocationHook
   - 包名：`com.locationhook.module`
   - SHA1获取方法：
     ```bash
     keytool -list -v -keystore ~/.android/debug.keystore
     # 默认密码：android
     ```
4. 复制得到的Key

---

### 第三步：配置项目

#### 1. 打开项目
```bash
# 启动Android Studio
# File → Open → 选择 LocationHook 文件夹
```

#### 2. 配置高德Key
在项目根目录创建 `local.properties` 文件：
```properties
sdk.dir=C:\\Users\\你的用户名\\AppData\\Local\\Android\\Sdk
AMAP_API_KEY=你的高德API Key
```

#### 3. 同步项目
```bash
# 点击 Android Studio 右上角的 "Sync Now" 按钮
# 或者点击：File → Sync Project with Gradle Files
```

---

### 第四步：编译打包

#### 方式一：通过Android Studio打包（推荐）

1. 选择Build类型：
   ```
   Build → Select Build Variant → debug
   ```

2. 开始编译：
   ```
   Build → Build Bundle(s) / APK(s) → Build APK(s)
   ```

3. 等待编译完成（约3-5分钟）

4. 编译成功后，右下角会弹出提示：
   ```
   Show in Explorer  ← 点击打开APK所在文件夹
   ```

#### 方式二：通过命令行打包

1. 打开终端，进入项目目录：
   ```bash
   cd C:\Users\Administrator\LocationHook
   ```

2. 执行打包命令：
   ```bash
   # Windows
   .\gradlew.bat assembleDebug
   
   # Mac/Linux
   ./gradlew assembleDebug
   ```

3. 等待编译完成

4. APK文件位置：
   ```
   app\build\outputs\apk\debug\app-debug.apk
   ```

---

### 第五步：安装和测试

#### 1. 安装APK到设备

**方式一：通过adb命令**
```bash
adb install app\build\outputs\apk\debug\app-debug.apk
```

**方式二：通过文件传输**
- 将APK文件复制到手机
- 在手机上点击安装

#### 2. LSPosed激活模块

1. 打开LSPosed Manager
2. 进入"模块"页面
3. 找到"定位模拟"，点击进入
4. 启用模块开关
5. 选择作用域：
   - 选择"系统框架"（必须）
   - 选择需要Hook的应用
6. 重启设备

#### 3. 使用方法

1. 打开"定位模拟"APP
2. 在地图上长按选择要模拟的位置
3. 打开顶部的"定位模拟"开关
4. 打开其他APP，查看位置是否已修改

---

## ❓ 常见问题解决

### Q1: 编译报错 "找不到符号" 或 "找不到类"
**解决**:
```bash
# 清除缓存后重新编译
File → Invalidate Caches / Restart → Invalidate and Restart

# 然后重新同步
File → Sync Project with Gradle Files
```

### Q2: 高德地图不显示，只有网格
**解决**:
1. 检查Key是否正确
2. 检查包名是否和高德控制台填写的一致
3. 检查SHA1是否正确（debug和release的SHA1不同）

### Q3: 编译报错 "资源找不到"
**解决**:
```bash
# 检查gradle版本是否匹配
# 项目使用gradle 8.2，检查Android Studio是否支持
# 检查SDK版本是否正确安装
```

### Q4: 打包后的APP无法安装
**解决**:
```bash
# 如果已经安装了旧版本，需要先卸载
adb uninstall com.locationhook.module

# 然后再安装
adb install app/build/outputs/apk/debug/app-debug.apk
```

### Q5: LSPosed提示模块未启用
**解决**:
1. 确认在LSPosed Manager中已勾选该模块
2. 确认已选择作用域（至少选择"系统框架"）
3. 必须重启设备才能生效

### Q6: 定位模拟不生效
**解决**:
1. 确认模块已在LSPosed中启用并重启
2. 在APP中打开开关，选择位置
3. 确认目标APP在LSPosed的作用域中
4. 清除目标APP的缓存和数据
5. 重启目标APP

---

## 📝 完整项目结构

```
LocationHook/
├── app/
│   ├── build.gradle
│   └── src/
│       └── main/
│           ├── AndroidManifest.xml
│           ├── assets/
│           │   └── xposed_init
│           ├── java/com/locationhook/module/
│           │   ├── LocationHookModule.java
│           │   ├── config/
│           │   │   └── HookConfig.java
│           │   ├── location/
│           │   │   ├── MockLocationProvider.java
│           │   │   ├── RouteSimulator.java
│           │   │   └── AMapLocationListenerWrapper.java
│           │   ├── amap/
│           │   │   └── AmapManager.java
│           │   ├── utils/
│           │   │   ├── LocationConverter.java
│           │   │   ├── XPreference.java
│           │   │   └── PermissionHelper.java
│           │   └── ui/
│           │       └── MainActivity.java
│           └── res/
│               ├── layout/
│               │   └── activity_main.xml
│               └── values/
│                   ├── colors.xml
│                   ├── strings.xml
│                   └── themes.xml
├── build.gradle
├── settings.gradle
├── gradle.properties
└── local.properties (需要创建)
```

---

## 🎉 总结

现在你已经拥有了：

✅ **完整的源代码**（核心Hook逻辑 + UI界面 + 高德地图集成）
✅ **详细打包指南**（从环境搭建到最终安装）
✅ **常见问题解决方案**（覆盖90%的常见问题）

### 下一步行动

1. **安装Android Studio**（如果还没有）
2. **申请高德地图Key**
3. **打开项目** → **配置Key** → **编译打包**
4. **安装测试**

### 需要我帮你做什么？

如果你在打包过程中遇到任何问题，可以告诉我：
1. 具体的错误信息截图
2. 出错的具体步骤
3. 你的环境配置（Android Studio版本、操作系统等）

我可以帮你排查问题！

---

**祝使用愉快！** 🚀
