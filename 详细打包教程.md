# LocationHook 详细打包&使用教程

## 📋 项目配置检查
所有核心文件完整，代码无问题，可以正常打包。

## 🚀 手动打包详细步骤（Android Studio版）

### 前提条件
- 已安装 Android Studio Ladybug+ 2024或更高
- 已配置 JDK 17+
- 已安装 Android SDK 36 (Android 16)

---

### 步骤1：打开项目
1. 打开Android Studio
2. 点击 **Open an existing project**
3. 选择文件夹：
   ```
   C:\Users\Administrator\.openclaw\workspace\projects\LocationHook
   ```
4. 点击OK

---

### 步骤2：Gradle同步
1. 等待3-5分钟，直到显示 **Gradle sync finished successfully**
2. 同步失败检查：
   - 网络是否正常
   - 尝试切换国内镜像源
   - 检查SDK版本是否正确

---

### 步骤3：配置高德地图Key（可选）
1. 打开 `local.properties`
2. 添加：
   ```properties
   AMAP_API_KEY=你的高德安卓端Key
   ```
3. Key申请地址：https://lbs.amap.com/
4. 包名：`com.locationhook.module`
5. Debug SHA1获取：`keytool -list -v -keystore ~/.android/debug.keystore` 密码：android

---

### 步骤4：打包APK
1. 点击菜单 **Build → Build Bundle(s)/APK(s) → Build APK(s)**
2. 等待2-3分钟构建完成
3. 成功后点击提示里的 **locate** 打开APK文件夹
4. APK位置：
   ```
   C:\Users\Administrator\.openclaw\workspace\projects\LocationHook\app\build\outputs\apk\debug\app-debug.apk
   ```

---

## 🧩 LSPosed激活方法
1. 手机安装 `app-debug.apk`
2. 打开LSPosed Manager → 模块 → 找到LocationHook
3. 开启开关，勾选作用域：
   - ✅ 系统框架（必须）
   - ✅ 需要模拟定位的应用
4. 重启手机生效

---

## 📱 使用方法
1. 打开APP
2. 地图上长按选择要模拟的位置
3. 打开顶部定位开关
4. 其他APP将获取到模拟的位置
5. 路线模拟功能：选择起点和终点，设置速度，点击开始

---

## ❓ 常见问题排查
| 问题 | 解决方案 |
|------|---------|
| Gradle同步失败 | 添加阿里云镜像源到settings.gradle：<br>```maven { url 'https://maven.aliyun.com/repository/public' }``` |
| 打包提示SDK 36不存在 | 打开SDK Manager安装Android 16 (API 36) |
| 安装APK提示签名不一致 | 卸载旧版本再安装 |
| 模块不生效 | 检查是否勾选"系统框架"，并重启手机 |
| 定位有抖动 | 在APP设置里调整抖动半径参数 |
| 地图不显示 | 检查高德Key是否配置正确，包名和SHA1是否匹配 |
